// Type re-exports so the SDK is the single import surface for clients.
export type {
  IntelligenceResult,
  QueryRequest,
  ChatRequest,
  SourceRef,
  SourceStreams,
  EvidenceItem,
  TimelineEvent,
  LawRef,
  BulletinRef,
  InstitutionDescriptor,
  HealthStatus,
} from "@intel.dom.gob/types";
